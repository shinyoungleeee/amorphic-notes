{
	"name": "amorphic-notes",
	"version": "0.1.0",
	"scripts": {
		"start": "lerna run start --stream",
		"compile": "lerna bootstrap && lerna run compile --stream"
	},
	"devDependencies": {
		"lerna": "^2.11.0"
	}
}
